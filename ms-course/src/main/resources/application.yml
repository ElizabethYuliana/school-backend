server:
  port: 7000
  servlet:
    context-path: /ms-course
spring:
  application:
    name: ms-course
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://20.232.168.239:5432/school_db
    username: postgres
    password: Develop2022$
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka
